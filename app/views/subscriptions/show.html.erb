<h1>Subscription</h1>

<p>
  <b>Name</b><br>
  <%= @subscription.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', subscriptions_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_subscription_path(@subscription), :class => 'btn' %>
  <%= link_to 'Delete', subscription_path(@subscription), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
